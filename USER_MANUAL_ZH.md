# alt+A 文本分析AI助手 - 用户手册

## 1. 概述

欢迎使用 alt+A 文本分析AI助手！这款浏览器扩展程序旨在帮助您快速理解网页上遇到的文本片段。只需将鼠标悬停在文本上并按下键盘快捷键（`Alt+A`，Mac 用户请使用 `Option+A`），该扩展就会将选定的文本发送给人工智能（AI）模型进行分析。AI 的解释或分析结果随后会直接显示在网页上，就在您选定文本的下方。

您可以自定义该扩展，以使用您偏好的人工智能服务、特定的AI模型，甚至可以调整给予AI的指令（即“提示”），以满足您的需求。

## 2. 安装

alt+A 文本分析AI助手是一款浏览器扩展程序。安装通常通过以下两种方式之一完成：

*   **从您的浏览器扩展商店安装：** 最简单的方法是从您的浏览器官方插件市场（如 Chrome Web Store、Firefox Add-ons 等）找到并安装该扩展。
*   **手动安装（开发者模式）：** 如果您拥有扩展文件，可以通过浏览器的开发者模式将其作为“已解压的扩展程序”加载。具体步骤因浏览器而异（例如，在 Chrome 中，请转到 `chrome://extensions`，启用“开发者模式”，然后点击“加载已解压的扩展程序”）。

安装后，您应该可以在浏览器的工具栏中看到该扩展的图标。

## 3. 配置

要使用 alt+A 文本分析AI助手，您首先需要配置您的AI服务详情。您可以点击浏览器工具栏中的扩展图标来访问扩展的设置。这将打开一个弹出窗口，其中包含两个主要视图：“设置” (Settings) 和 “模型预设” (Model Presets)。

### 3.1. API 设置

本部分对于扩展与AI服务通信至关重要。

1.  **访问设置：** 点击浏览器工具栏中的扩展图标。默认情况下，弹出窗口应打开“设置”视图。如果您在“模型预设”视图中，请点击顶部的“Settings”按钮。
2.  **API 端点 (API Endpoint)：**
    *   在“API Endpoint”字段中，输入您的AI服务提供商提供的用于发出API请求的完整URL。例如，对于OpenAI，它可能看起来像 `https://api.openai.com/v1/chat/completions`。
3.  **API 密钥 (API Key)：**
    *   在“API Key”字段中，输入您的唯一API密钥。此密钥用于向AI服务验证您的请求。请妥善保管此密钥。为保护隐私，输入字段通常会隐藏您的密钥。
4.  **保存设置：** 输入端点和密钥后，点击 **“Save Settings”** (保存设置) 按钮。系统将短暂显示确认消息，如“API Settings saved!”。

*重要提示：* 如果没有有效的API端点和密钥，扩展将无法分析文本。

### 3.2. 模型预设

模型预设允许您保存并快速切换您可能使用的不同AI模型名称。

1.  **访问模型预设视图：** 在扩展弹出窗口中，点击顶部的 **“Model Presets”** (模型预设) 按钮。
2.  **定义预设：** 您将看到五个标记为“Preset 1”到“Preset 5”的输入字段。
    *   在每个字段中，您可以输入AI服务提供的特定模型名称（例如，“gpt-4o”，“gpt-3.5-turbo”，“claude-3-opus-20240229”等）。
    *   您可以定义多达五个，或者如果您愿意，也可以只定义一个或两个。
3.  **保存预设：** 输入所需的模型名称后，点击 **“Save Model Presets”** (保存模型预设) 按钮。系统将显示确认消息“Model presets saved!”。

### 3.3. 选择模型

定义模型预设后，您可以在“设置”视图中选择要用于分析的模型。

1.  **访问设置视图：** 如果您尚未在该视图，请点击弹出窗口中的“Settings”按钮。
2.  **选择一个预设：** 您会看到标有（例如“P1”、“P2”，如果预设已命名，则为“P1: gpt-4o”）的按钮。这些按钮对应您定义的预设。点击您希望使用的模型的按钮。
    *   所选预设按钮通常会以粗体或高亮显示。
3.  **显示选定模型：** 您刚选择的模型的名称将显示在“Selected Model:” (选定模型：)下方。如果预设为空，则可能显示“Preset not set”(预设未设置)。

扩展将使用此选定模型进行所有后续分析。

### 3.4. 自定义提示模板

提示模板告诉AI您希望它对所选文本执行何种类型的分析。

1.  **访问自定义提示：** 在“设置”视图中，找到“Custom Prompt Template” (自定义提示模板) 部分。
2.  **编辑模板：**
    *   您会看到一个文本区域，您可以在其中键入或粘贴您希望提供给AI的指令。
    *   **至关重要的是，您的提示必须包含占位符 `{{TEXT_TO_ANALYZE}}`。** 这个确切的占位符将被您在网页上选择的实际文本所取代。
    *   例如，一个简单的提示可以是：“用简单的术语解释这段文字：{{TEXT_TO_ANALYZE}}”
3.  **默认提示：** 默认提示专为英语学习者设计。它要求AI识别所选文本中的英语短语和固定搭配，并用简单的英语进行解释，最终答案以中文提供。您可以随时恢复到此默认设置。
4.  **保存或重置：**
    *   要保存您的自定义提示，请点击 **“Save Prompt”** (保存提示) 按钮。
    *   要恢复到默认提示，请点击 **“Reset to Default”** (重置为默认值) 按钮。
    *   状态消息将确认操作。

### 3.5. 测试API配置

设置API端点、密钥并选择模型后，最好测试一下一切是否正常工作。

1.  **访问测试按钮：** 在“设置”视图中，找到 **“Test API Configuration”** (测试API配置) 按钮。
2.  **运行测试：** 点击该按钮。扩展将尝试使用当前的API设置和选定的模型向您配置的AI服务发送一个非常简单的请求（如“Hello!”）。
3.  **解读结果：**
    *   消息将显示在“Test Result” (测试结果) 区域。
    *   **成功：** 如果您看到类似“Success: API connection successful!”的消息，则您的设置很可能是正确的。
    *   **错误：** 如果出现问题，将显示错误消息（例如，无效API密钥会显示“Error: API returned status: 401”，或网络问题会显示“Error: Network error...”）。此消息可以帮助您诊断问题。

### 3.6. 响应处理（过滤思考过程）

此功能允许您在AI的响应显示在页面上之前，自动隐藏或移除其中冗长的“思考过程”、调试信息或其他指定片段。这有助于保持最终分析结果的简洁，并专注于主要答案。

这些设置位于弹出窗口的“设置”(Settings)视图中，通常在“自定义提示模板”(Custom Prompt Template)部分的下方。

1.  **启用过滤 (Enable Filtering)：**
    *   勾选 **“Enable filtering of thought process”** (启用过滤思考过程) 复选框以开启此功能。如果未勾选，则无论下面定义的符号如何，都不会进行过滤。
2.  **起始符号 (Start Symbol)：**
    *   在“Start Symbol” (起始符号) 输入字段中，定义标记要移除文本片段开始的确切字符序列。
    *   例如，默认值为 `"<think>"`。许多AI模型使用此标签或类似标签来封装其内部推理步骤。
3.  **结束符号 (End Symbol)：**
    *   在“End Symbol” (结束符号) 输入字段中，定义标记要移除文本片段结尾的确切字符序列。
    *   例如，默认值为 `"</think>"`。
4.  **工作原理：**
    *   启用后，扩展将在AI的响应中查找以您定义的“起始符号”开始并以“结束符号”结束的文本片段。
    *   在这些符号之间找到的任何文本（包括符号本身）都将在显示到您的网页上之前从分析中移除。
    *   如果在响应中仅找到“起始符号”而没有匹配的“结束符号”，则从该点开始的文本可能会保持可见，以防止意外数据丢失（这取决于具体的响应结构）。
    *   如果在启用过滤的情况下将“起始符号”或“结束符号”字段留空，扩展将自动使用默认值（`"<think>"` 和 `"</think>"`）进行过滤。
5.  **保存：** 当您点击主 **“Save Settings”** (保存设置) 按钮时，这些响应处理设置将与您的API详细信息一起保存。

## 4. 使用方法

配置完成后，使用该扩展非常简单。

### 4.1. 选择文本

该扩展适用于鼠标光标当前悬停的HTML元素的文本内容。只需将鼠标指针移到您想要分析的段落、句子甚至单个词语上。

### 4.2. 触发分析

当鼠标悬停在所需文本上时：
*   同时按下键盘上的 **`Alt+A`** 键。
*   如果您使用的是 Mac，快捷键是 **`Option+A`**。

然后，扩展将从悬停的元素中捕获文本并将其发送进行分析。

### 4.3. 查看分析结果

稍等片刻（处理时间取决于AI服务和请求的复杂性），分析结果将直接显示在网页上。
*   一个新的框或区域将插入到包含您所选文本的HTML元素的正下方。
*   这个框将包含来自AI的响应。

### 4.4. 关闭分析结果

出现的分析框还将包含一个 **“Close Analysis”** (关闭分析) 按钮。点击此按钮将从页面中移除分析显示。

## 5. 常见问题解答

以下是一些常见问题及其解决方法：

*   **“Error: API not configured. Please set it in the extension popup.” (显示在网页上)：**
    *   这意味着您尚未在扩展的设置中保存您的API端点和密钥。
    *   **解决方法：** 点击浏览器工具栏中的扩展图标，转到“设置”，输入您的API端点和密钥，然后点击“Save Settings”。

*   **分析或测试过程中的API错误（例如，“Error: API returned status: 401”，“Error: Network error...”，“Error calling LLM API: ...”）：**
    *   这些表明您的API配置或与AI服务的连接存在问题。
    *   **尝试的解决方法：**
        *   仔细检查您在扩展设置中输入的API密钥和API端点是否正确。
        *   确保您选择的模型预设对您的API提供商有效，并且您的API密钥具有相应的权限。
        *   验证您的互联网连接是否正常。
        *   使用设置中的“Test API Configuration”按钮获取更具体的反馈。
        *   查阅您的AI服务提供商的文档，了解特定错误代码（如401、403、429、500）的含义。

*   **按 Alt+A 时没有选择或分析文本：**
    *   确保在按下热键时，您的鼠标光标正悬停在网页上*包含文本*的元素上。如果您悬停在图像或空白区域上，可能没有文本可供分析。
    *   尝试将鼠标悬停在页面上的其他文本片段上。

*   **分析结果未出现，或其他意外行为：**
    *   **检查权限：** 确保扩展具有在当前网页上操作的必要权限。有时，浏览器安全设置或其他扩展可能会造成干扰。
    *   **重新加载页面：** 尝试重新加载网页，然后再次尝试分析。如果扩展刚刚安装或更新，通常需要重新加载页面才能使内容脚本正常加载。
    *   **检查错误消息：** 查看扩展显示的任何错误消息（无论是在页面上还是在弹出窗口的测试结果区域）。
    *   **开发者控制台：** 对于更高级的用户，请检查浏览器的开发者控制台（通常使用F12键打开）中与扩展相关的错误消息。

---
我们希望本手册能帮助您充分利用 alt+A 文本分析AI助手！
